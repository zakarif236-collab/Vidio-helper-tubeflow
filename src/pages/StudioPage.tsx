// StudioPage is the primary page for the 'studio' experience.
// The current App implementation lives in `src/App.tsx`.
// We re-export it here for clean folder-based page organization.

export { default } from '../App';
