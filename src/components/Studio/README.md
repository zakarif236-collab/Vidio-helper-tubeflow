# Studio Components

This folder is reserved for reusable studio-specific components.
Use this directory for components shared within the Studio page (e.g., thumbnail editor, canvas tools, game widgets, etc.).
